# دليل نظام فحص المطابقة المحاسبية

## نظرة عامة

تم تطوير نظام متكامل للتحقق من توازن القيود المحاسبية في التطبيق، يضمن أن جميع الحركات المالية متوازنة ومطابقة للمعايير المحاسبية.

---

## المبدأ المحاسبي الأساسي

في أي نظام محاسبي صحيح، يجب أن يكون:

```
مجموع المدين (Debits) = مجموع الدائن (Credits)
```

**المدين:** الحركات الصادرة (`outgoing`) - الأموال التي تخرج من الحسابات
**الدائن:** الحركات الواردة (`incoming`) - الأموال التي تدخل إلى الحسابات

---

## مثال توضيحي

### السيناريو

**تحويل 6000$ من جلال إلى عماد مع عمولة 150$**

### القيود المحاسبية

| الحساب | النوع | المبلغ | المدين/الدائن |
|--------|------|-------|---------------|
| جلال | outgoing | 6000$ | **مدين** |
| عماد | incoming | 5850$ | **دائن** |
| الأرباح والخسائر | incoming | 150$ | **دائن** |

### المطابقة

```
المدين: 6000$
الدائن: 5850$ + 150$ = 6000$
النتيجة: متوازن ✓
```

---

## مكونات النظام

### 1. قاعدة البيانات

#### VIEW: `accounting_balance_check`

يوفر ملخصاً شاملاً للمطابقة المحاسبية لكل عملة:

```sql
SELECT * FROM accounting_balance_check;
```

**الأعمدة:**
- `currency`: العملة
- `total_debits`: إجمالي المدين (outgoing)
- `total_credits`: إجمالي الدائن (incoming)
- `net_balance`: الفرق (يجب أن يكون 0)
- `is_balanced`: حالة التوازن (true/false)
- `absolute_difference`: القيمة المطلقة للفرق
- `debit_count`: عدد الحركات المدينة
- `credit_count`: عدد الحركات الدائنة

#### دالة: `get_accounting_balance_summary()`

توفر ملخصاً سريعاً لحالة المطابقة في جميع العملات:

```sql
SELECT * FROM get_accounting_balance_summary();
```

**الحقول المرجعة:**
- `total_currencies`: إجمالي عدد العملات
- `balanced_currencies`: عدد العملات المتوازنة
- `unbalanced_currencies`: عدد العملات غير المتوازنة
- `overall_status`: الحالة العامة ('متوازن ✓' أو 'غير متوازن ⚠')

### 2. الواجهة الأمامية

#### صفحة الإحصائيات (`app/statistics.tsx`)

تم إضافة قسم جديد "المطابقة المحاسبية" يعرض:

**1. بانر الملخص:**
- أيقونة الحالة (✓ أو ⚠)
- الحالة العامة
- عدد العملات المتوازنة/غير المتوازنة

**2. كارد لكل عملة:**
- رمز العملة واسمها
- badge يوضح حالة التوازن
- إجمالي المدين مع عدد الحركات
- إجمالي الدائن مع عدد الحركات
- المعادلة المحاسبية
- الفرق (في حالة عدم التوازن)

**3. شرح توضيحي:**
- نص يشرح المبدأ المحاسبي الأساسي

---

## الألوان المستخدمة

### الحالة المتوازنة (Balanced)
- اللون الأساسي: `#10B981` (أخضر)
- الخلفية: `#ECFDF5` (أخضر فاتح)
- الحد: `#10B981`

### الحالة غير المتوازنة (Unbalanced)
- اللون الأساسي: `#F59E0B` (برتقالي/أصفر)
- الخلفية: `#FFFBEB` (أصفر فاتح)
- الحد: `#FCD34D` (أصفر غامق)

### الأيقونات
- المدين (outgoing): أحمر `#EF4444` مع `TrendingDown`
- الدائن (incoming): أخضر `#10B981` مع `TrendingUp`
- المطابقة: بنفسجي `#8B5CF6` مع `Scale`
- التحذير: برتقالي `#F59E0B` مع `AlertCircle`
- النجاح: أخضر `#10B981` مع `CheckCircle`

---

## كيفية الاستخدام

### 1. الوصول إلى صفحة المطابقة

1. افتح التطبيق
2. اذهب إلى تبويب "الإعدادات"
3. اضغط على "الإحصائيات"
4. قم بالتمرير إلى قسم "المطابقة المحاسبية"

### 2. قراءة النتائج

#### إذا كان النظام متوازناً ✓

سترى:
- بانر أخضر مع علامة ✓
- نص "متوازن ✓"
- جميع الكاردات بحدود خضراء
- المعادلة متساوية لكل عملة

**هذا يعني:** النظام سليم ومطابق للمعايير المحاسبية

#### إذا كان النظام غير متوازن ⚠

سترى:
- بانر أصفر مع علامة ⚠
- نص "غير متوازن ⚠"
- الكاردات غير المتوازنة بحدود صفراء
- عرض الفرق بوضوح
- المعادلة غير متساوية

**هذا يعني:** هناك مشكلة في القيود المحاسبية تحتاج إلى فحص

### 3. حل مشاكل عدم التوازن

إذا وجدت أن النظام غير متوازن:

1. **تحقق من الحركات الأخيرة:**
   - اذهب إلى صفحة تفاصيل العملاء
   - راجع الحركات الحديثة
   - ابحث عن حركات غير صحيحة أو مكررة

2. **تحقق من العمولات:**
   - تأكد من تسجيل العمولات بشكل صحيح
   - راجع حساب الأرباح والخسائر

3. **اتصل بالدعم الفني:**
   - إذا لم تتمكن من حل المشكلة
   - وفر معلومات عن الفرق والعملة المتأثرة

---

## الفحوصات التلقائية

النظام يقوم تلقائياً بـ:

✅ استبعاد حركات العمولة المنفصلة (لتجنب الحساب المزدوج)
✅ حساب الأرصدة بالمبالغ الصافية
✅ تجميع الحركات حسب العملة
✅ التحقق من التوازن لكل عملة على حدة

---

## استعلامات SQL مفيدة

### فحص المطابقة لعملة معينة

```sql
SELECT * FROM accounting_balance_check
WHERE currency = 'USD';
```

### الحصول على ملخص سريع

```sql
SELECT * FROM get_accounting_balance_summary();
```

### فحص جميع الحركات لعملة معينة

```sql
SELECT
  am.movement_type,
  am.amount,
  c.name as customer_name,
  am.created_at
FROM account_movements am
JOIN customers c ON am.customer_id = c.id
WHERE
  am.currency = 'USD'
  AND (am.is_commission_movement IS NULL OR am.is_commission_movement = false)
ORDER BY am.created_at DESC;
```

---

## الأسئلة الشائعة

### س1: لماذا يظهر النظام غير متوازن؟

**ج:** قد يكون هناك عدة أسباب:
1. حركة لم يتم تسجيلها بشكل كامل
2. خطأ في تسجيل العمولة
3. حذف حركة بشكل غير صحيح
4. مشكلة في التحويل الداخلي

### س2: هل يؤثر عدم التوازن على الأرصدة؟

**ج:** نعم، عدم التوازن يعني أن هناك خطأ في الأرصدة، مما قد يؤدي إلى:
- أرصدة غير صحيحة للعملاء
- تقارير مالية خاطئة
- مشاكل في المحاسبة

### س3: كيف يمكن منع حدوث عدم التوازن؟

**ج:** النظام مصمم لمنع ذلك تلقائياً عن طريق:
- استخدام transactions في قاعدة البيانات
- تسجيل العمولات تلقائياً
- التحقق من صحة البيانات عند الإدخال

### س4: هل حركات العمولة تؤثر على المطابقة؟

**ج:** لا، حركات العمولة المنفصلة (is_commission_movement = true) مستبعدة من الحساب، لأنها حركات داخلية تُسجل فقط في حساب الأرباح والخسائر.

---

## الملفات المتأثرة

### قاعدة البيانات
- `supabase/migrations/*_add_accounting_balance_check_view.sql` - VIEW والدالة الجديدة

### Types
- `types/database.ts` - Interfaces للمطابقة المحاسبية

### Services
- `services/statisticsService.ts` - دوال جلب بيانات المطابقة

### UI
- `app/statistics.tsx` - واجهة عرض المطابقة المحاسبية

---

## الخلاصة

نظام فحص المطابقة المحاسبية يوفر:

✅ **شفافية كاملة:** رؤية واضحة لحالة التوازن المحاسبي
✅ **كشف تلقائي:** اكتشاف المشاكل فوراً
✅ **دقة عالية:** حسابات دقيقة بناءً على المعايير المحاسبية
✅ **سهولة الاستخدام:** واجهة واضحة ومفهومة
✅ **تقارير شاملة:** معلومات تفصيلية لكل عملة

**النظام جاهز للاستخدام ومُختبر بالكامل!** ✨

---

**آخر تحديث:** 9 يناير 2025
