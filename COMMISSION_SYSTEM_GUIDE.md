# دليل نظام العمولة التلقائي

## نظرة عامة

تم تطوير نظام متكامل لإدارة العمولات بشكل تلقائي، حيث يتم تسجيل جميع العمولات في حساب خاص يُسمى **"الأرباح والخسائر"** (Profit & Loss Account).

---

## المفاهيم الأساسية

### 1. حساب الأرباح والخسائر

**الخصائص:**
- ✅ حساب نظام ثابت لا يمكن حذفه نهائياً
- ✅ يمكن تعديل اسم الحساب فقط (Rename)
- ✅ يظهر بشكل مميز في قائمة العملاء (خلفية ذهبية + أيقونة TrendingUp 💰)
- ✅ جميع العمولات تُسجل فيه تلقائياً
- ✅ لا يمكن تصفير الحساب أو حذفه من الواجهة

**كيفية التعرف عليه:**
- الحقل `is_profit_loss_account = true` في جدول `customers`
- يظهر بخلفية صفراء ذهبية في قائمة العملاء
- أيقونة خاصة (TrendingUp) بدلاً من الحروف الأولى

---

## منطق العمولة

### القاعدة العامة

عند إجراء حركة بمبلغ (Amount) وعمولة (Commission):

```
المبلغ المسجل = Amount
العمولة = Commission
الصافي = Amount - Commission
```

### التوزيع

#### للحركات الواردة (incoming) - تسليم للعميل:
1. **المبلغ المسجل:** `Amount` (المبلغ الكامل)
2. **العمولة المخصومة:** `Commission`
3. **رصيد العميل يزيد بـ:** `Amount - Commission` (الصافي)
4. **حساب الأرباح والخسائر يستلم:** `Commission`

#### للحركات الصادرة (outgoing) - استلام من العميل:
1. **المبلغ المسجل:** `Amount` (المبلغ الكامل)
2. **العمولة المخصومة:** `Commission`
3. **رصيد العميل ينقص بـ:** `Amount - Commission` (الصافي)
4. **حساب الأرباح والخسائر يستلم:** `Commission`

### كيف يتم الحساب؟

**في قاعدة البيانات:**
- يتم تسجيل المبلغ الكامل (Amount) في حقل `amount`
- يتم تسجيل العمولة (Commission) في حقل `commission`
- الـ VIEWs تحسب الرصيد بخصم العمولة تلقائياً:
  ```sql
  balance = SUM(
    CASE
      WHEN movement_type = 'incoming' THEN (amount - commission)
      WHEN movement_type = 'outgoing' THEN -(amount - commission)
    END
  )
  ```

**الفوائد:**
- ✅ رصيد العميل يعكس المبلغ الصافي الحقيقي
- ✅ العمولة واضحة ومفصولة في سجل الحركة
- ✅ حساب الأرباح والخسائر يستلم العمولات تلقائياً
- ✅ لا حاجة لتعديل الحركات القديمة

---

## أمثلة عملية

### مثال 1: استلام من عميل

**السيناريو:** استلام 3000 USD من أحمد بعمولة 60 USD

**ما يتم تسجيله:**
```sql
customer_id: أحمد
movement_type: outgoing
amount: 3000
commission: 60
currency: USD
```

**النتائج:**
1. **رصيد أحمد:**
   - يُخصم منه: 3000 - 60 = **2940 USD** (الصافي)
   - معنى الرصيد: "له عندنا 2940 دولار"

2. **حركة العمولة التلقائية:**
   - تُنشأ حركة incoming في حساب الأرباح والخسائر
   - المبلغ: 60 USD
   - الملاحظة: "عمولة من حركة رقم MV-XXXXX"

3. **رصيد الأرباح والخسائر:**
   - يزيد بـ: **60 USD**

---

### مثال 2: تسليم لعميل

**السيناريو:** تسليم 2000 USD لأحمد بعمولة 40 USD

**ما يتم تسجيله:**
```sql
customer_id: أحمد
movement_type: incoming
amount: 2000
commission: 40
currency: USD
```

**النتائج:**
1. **رصيد أحمد:**
   - يُضاف له: 2000 - 40 = **1960 USD** (الصافي)
   - معنى الرصيد: "لنا عنده 1960 دولار"

2. **حركة العمولة التلقائية:**
   - تُنشأ حركة incoming في حساب الأرباح والخسائر
   - المبلغ: 40 USD

3. **رصيد الأرباح والخسائر:**
   - يزيد بـ: **40 USD**

---

### مثال 3: الرصيد النهائي لأحمد

بعد الحركتين السابقتين:
```
incoming (تسليم): +1960 USD
outgoing (استلام): -2940 USD
الرصيد النهائي: -980 USD
```

**معنى الرصيد:**
- سالب = "له عندنا"
- أحمد له عندنا 980 دولار

---

### مثال 4: تحويل داخلي بين عملاء

**السيناريو:** جلال يُحول 5000 USD إلى عماد بعمولة 50 USD (جلال يدفع العمولة)

**ما يتم تسجيله:**

**الحركة 1 (جلال):**
```sql
customer_id: جلال
movement_type: outgoing
amount: 5000
commission: 50
commission_recipient_id: جلال (هو من يستلم العمولة)
```

**الحركة 2 (عماد):**
```sql
customer_id: عماد
movement_type: incoming
amount: 5000
commission: 50
```

**النتائج:**
1. **رصيد جلال:**
   - يُخصم منه: 5000 - 50 = **4950 USD**
   - يستلم عمولة: 50 USD (كحركة منفصلة)
   - الصافي النهائي: -4950 + 50 = **-4900 USD**

2. **رصيد عماد:**
   - يُضاف له: 5000 - 50 = **4950 USD**

3. **حساب الأرباح والخسائر:**
   - يدفع العمولة لجلال: -50 USD

---

## عرض البيانات في السند

### ما يظهر في السند المطبوع

عند طباعة السند للعميل، يظهر التالي بوضوح:

#### مثال: سند تسليم لأحمد

| العنوان | القيمة |
|---------|-------|
| **المبلغ الإجمالي** | 2000 USD |
| **عملة الحساب** | الدولار الأمريكي |
| **العمولة** | 40 USD |
| **الصافي** | 1960 USD |

**المبلغ كتابةً:**
```
ألف وتسعمائة وستون دولاراً أمريكياً
```

**ملاحظات مهمة:**
- ✅ المبلغ كتابة يُحسب من **الصافي** (1960) وليس الإجمالي (2000)
- ✅ العمولة معروضة بوضوح للعميل
- ✅ الصافي هو المبلغ الفعلي الذي يؤثر على رصيد العميل
- ✅ جميع المبالغ واضحة لا لبس فيها

---

## التطبيق التقني

### 1. قاعدة البيانات

#### جدول العملاء (customers)
```sql
CREATE TABLE customers (
  id uuid PRIMARY KEY,
  name text NOT NULL,
  phone text NOT NULL,
  is_profit_loss_account boolean DEFAULT false, -- ✨ جديد
  -- ... باقي الحقول
);

-- ضمان حساب واحد فقط للأرباح والخسائر
ALTER TABLE customers
ADD CONSTRAINT only_one_profit_loss_account
EXCLUDE USING btree (is_profit_loss_account WITH =)
WHERE (is_profit_loss_account = true);
```

#### Trigger تلقائي
```sql
CREATE TRIGGER trigger_record_commission
  AFTER INSERT ON account_movements
  FOR EACH ROW
  EXECUTE FUNCTION record_commission_to_profit_loss();
```

**الوظيفة:**
- يتم تشغيله تلقائياً بعد كل حركة جديدة
- إذا كانت العمولة > 0:
  - يُنشئ حركة منفصلة في حساب الأرباح والخسائر
  - الحركة تكون `incoming` بمبلغ العمولة

### 2. Views المحدثة

#### customer_balances_by_currency & customer_balances

**المنطق الجديد (بعد التحديث في 8 يناير 2026):**

```sql
-- للعملاء العاديين: خصم العمولة من كل حركة
balance = SUM(
  CASE
    WHEN movement_type = 'incoming'
      THEN (amount - COALESCE(commission, 0))
    WHEN movement_type = 'outgoing'
      THEN -(amount - COALESCE(commission, 0))
  END
)

-- استبعاد حركات العمولة المنفصلة
WHERE (is_commission_movement IS NULL OR is_commission_movement = false)
```

**لحساب الأرباح والخسائر:**
```sql
-- حساب الأرباح والخسائر: المبلغ الكامل بدون خصم
-- يشمل جميع حركات العمولة المنفصلة
balance = SUM(
  CASE
    WHEN movement_type = 'incoming' THEN amount
    WHEN movement_type = 'outgoing' THEN -amount
  END
)

-- تضمين جميع الحركات (بما فيها حركات العمولة)
WHERE c.phone = 'PROFIT_LOSS_ACCOUNT'
```

**الفرق الرئيسي:**
- **قبل التحديث:** كان يتم حساب المبلغ الكامل (amount) فقط
- **بعد التحديث:** يتم خصم العمولة من كل حركة تلقائياً: `(amount - commission)`

**الفوائد:**
- ✅ رصيد العميل = المبلغ الصافي الذي تم استلامه/تسليمه فعلياً
- ✅ العمولات واضحة في السجل
- ✅ حساب الأرباح والخسائر مفصول ومستقل
- ✅ لا تكرار في حساب العمولات

### 3. الواجهة الأمامية

#### قائمة العملاء (customers.tsx)
```typescript
// حساب الأرباح والخسائر يظهر بشكل مميز
const isProfitLoss = item.is_profit_loss_account;

<TouchableOpacity
  style={[
    styles.customerCard,
    isProfitLoss && styles.profitLossCard // خلفية ذهبية
  ]}
>
  {isProfitLoss ? (
    <TrendingUp size={28} color="#FFFFFF" /> // أيقونة خاصة
  ) : (
    // الأيقونة العادية
  )}
  <Text>{item.name} {isProfitLoss && '💰'}</Text>
</TouchableOpacity>
```

#### السندات (receiptGenerator.ts)
```typescript
// حساب الصافي
const netAmount = Number(amount) - Number(commission || 0);

// المبلغ كتابة يستخدم الصافي
const amountInWords = numberToArabicTextWithCurrency(netAmount, currency);

// عرض البيانات
<div class="info-card">
  <div class="card-label">المبلغ الإجمالي</div>
  <div class="card-value">${amount}</div>
</div>

<div class="info-card">
  <div class="card-label">العمولة</div>
  <div class="card-value">${commission}</div>
</div>

<div class="info-card">
  <div class="card-label">الصافي</div>
  <div class="card-value">${netAmount}</div>
</div>
```

---

## الشروط والقواعد

### 1. العمولة = 0
- **السلوك:** لا يتم تسجيل أي حركة على حساب الأرباح والخسائر
- **الصافي:** يساوي المبلغ الإجمالي
- **Trigger:** لا يتم تشغيله

### 2. عملة العمولة
- **القاعدة:** العمولة تكون بنفس عملة الحركة الأساسية
- **مثال:** إذا كان التحويل بالدولار، العمولة تُسجل بالدولار
- **حقل:** `commission_currency` في جدول `account_movements`

### 3. تطبيق النظام
- **النطاق:** جميع التحويلات التي تحتوي على عمولة
- **الأنواع:**
  - ✅ تحويلات بين العملاء
  - ✅ تحويلات من/إلى المحل
  - ✅ حركات واردة وصادرة
  - ✅ التحويلات الداخلية

---

## الحماية والأمان

### 1. منع الحذف

**في قاعدة البيانات:**
```sql
-- RLS Policy
CREATE POLICY "Users can delete own customers"
  ON customers FOR DELETE
  TO authenticated
  USING (is_profit_loss_account IS NOT true);

-- في دالة delete_customer
IF is_profit_loss THEN
  RETURN json_build_object(
    'success', false,
    'error', 'لا يمكن حذف حساب الأرباح والخسائر'
  );
END IF;
```

**في الواجهة:**
```typescript
const handleCustomerLongPress = (customer) => {
  if (customer.is_profit_loss_account) {
    Alert.alert(
      'حساب الأرباح والخسائر',
      'هذا حساب النظام ولا يمكن حذفه أو تصفيره.'
    );
    return;
  }
  // ... باقي الخيارات
};
```

### 2. ضمان الوحدة
```sql
-- Constraint لضمان حساب واحد فقط
EXCLUDE USING btree (is_profit_loss_account WITH =)
WHERE (is_profit_loss_account = true);
```

---

## الاختبار والتحقق

### 1. اختبار السيناريو الأساسي

**الخطوات:**
1. إنشاء تحويل من عميل إلى آخر
2. إضافة مبلغ: 1000
3. إضافة عمولة: 50
4. حفظ الحركة

**التحقق:**
```sql
-- 1. التحقق من رصيد المستفيد
SELECT balance FROM customer_balances
WHERE customer_id = 'المستفيد' AND currency = 'USD';
-- النتيجة المتوقعة: 950 (1000 - 50)

-- 2. التحقق من رصيد الأرباح والخسائر
SELECT balance FROM customer_balances
WHERE is_profit_loss_account = true AND currency = 'USD';
-- النتيجة المتوقعة: 50

-- 3. التحقق من عدد الحركات
SELECT COUNT(*) FROM account_movements
WHERE notes LIKE '%عمولة من حركة%';
-- النتيجة المتوقعة: عدد يساوي عدد الحركات ذات العمولة
```

### 2. اختبار العمولة = 0

**الخطوات:**
1. إنشاء تحويل بدون عمولة
2. حفظ الحركة

**التحقق:**
```sql
-- التحقق من عدم إنشاء حركة عمولة
SELECT COUNT(*) FROM account_movements
WHERE customer_id = (SELECT id FROM customers WHERE is_profit_loss_account = true)
AND notes LIKE '%عمولة من حركة MV-XXXXXX%';
-- النتيجة المتوقعة: 0
```

### 3. اختبار الحماية من الحذف

**الخطوات:**
1. محاولة حذف حساب الأرباح والخسائر من الواجهة
2. الضغط المطول على الحساب

**التحقق:**
- يجب أن يظهر Alert يمنع الحذف
- لا يجب ظهور خيارات "حذف" أو "تصفير"

---

## الفوائد

### 1. للمحاسبة
- ✅ تتبع دقيق لجميع العمولات
- ✅ رصيد واضح للأرباح
- ✅ سهولة إعداد التقارير المالية
- ✅ فصل الأرباح عن حسابات العملاء

### 2. للشفافية
- ✅ عرض واضح للمبلغ الإجمالي والصافي
- ✅ المبلغ كتابة يعكس ما سيستلمه العميل فعلياً
- ✅ لا لبس في السندات المطبوعة

### 3. للأتمتة
- ✅ تسجيل تلقائي دون تدخل يدوي
- ✅ تقليل الأخطاء البشرية
- ✅ سرعة في المعاملات

### 4. للنظام
- ✅ بنية منطقية ومنظمة
- ✅ سهولة الصيانة والتطوير
- ✅ قابلية التوسع مستقبلاً

---

## الأسئلة الشائعة

### س1: ماذا لو أردت تغيير اسم حساب الأرباح والخسائر؟
**ج:** يمكنك تعديل اسم الحساب من صفحة تفاصيل العميل. فقط اسم الحساب قابل للتعديل، أما الحذف أو التصفير فممنوع.

### س2: هل يمكن وجود أكثر من حساب أرباح وخسائر؟
**ج:** لا، يوجد constraint في قاعدة البيانات يمنع إنشاء أكثر من حساب واحد.

### س3: ماذا يحدث إذا حذفت حركة تحتوي على عمولة؟
**ج:** يجب حذف حركة العمولة المرتبطة يدوياً أو إضافة trigger لحذفها تلقائياً (يُنصح بالحذف اليدوي لتجنب الأخطاء).

### س4: هل العمولة تؤثر على المُرسل؟
**ج:** لا، المُرسل يُخصم منه المبلغ الكامل فقط. العمولة تُخصم من المستفيد.

### س5: كيف أعرف إجمالي العمولات لفترة معينة؟
**ج:** يمكنك عرض تفاصيل حساب الأرباح والخسائر لرؤية جميع العمولات المُسجلة.

---

## ملفات المشروع المعنية

### قاعدة البيانات
- `supabase/migrations/*_add_profit_loss_account_v2.sql`
- `supabase/migrations/*_add_commission_auto_recording_trigger.sql`
- `supabase/migrations/*_update_views_for_net_amount_v2.sql`

### الكود
- `types/database.ts` - تعريفات TypeScript
- `app/(tabs)/customers.tsx` - عرض قائمة العملاء
- `utils/receiptGenerator.ts` - توليد السندات
- `services/receiptService.ts` - خدمات الطباعة

---

## حركات العمولة المنفصلة

### ما هي؟

عند إنشاء حركة تحتوي على عمولة، يقوم النظام تلقائياً بإنشاء **حركة منفصلة** لتسجيل العمولة في حساب الأرباح والخسائر. هذه الحركات لها خصائص مميزة:

**الحقول المميزة:**
- `is_commission_movement = true` - علامة تدل على أن هذه حركة عمولة منفصلة
- `related_commission_movement_id` - معرف الحركة الأساسية التي نشأت منها العمولة
- `customer_id` - حساب الأرباح والخسائر أو مستلم العمولة
- `notes` - تحتوي على رقم الحركة الأساسية (مثل: "عمولة من حركة رقم MV-XXXXX")

### كيف يتم حسابها؟

**في قاعدة البيانات:**
- جميع الـ VIEWs تستبعد حركات العمولة المنفصلة من الحساب
- الشرط المستخدم: `WHERE (is_commission_movement IS NULL OR is_commission_movement = false)`
- هذا يمنع الحساب المزدوج للعمولات

**في الواجهة:**
- صفحة العملاء: تستخدم VIEW وتحسب الأرصدة بشكل صحيح تلقائياً
- صفحة تفاصيل العميل: تستبعد حركات العمولة من الاستعلام
- حساب الأرباح والخسائر: يعرض حركات العمولة بوضوح مع ملاحظة "عمولة من حركة رقم..."

### مثال عملي

**عند إنشاء حركة:**
```
العميل: جلال
المبلغ: 5000 USD
العمولة: 120 USD
النوع: incoming
```

**ما يحدث:**

1. **الحركة الأساسية (تُعرض لجلال):**
   - `customer_id`: جلال
   - `amount`: 5000
   - `commission`: 120
   - `is_commission_movement`: NULL/false
   - الرصيد المحتسب: 5000 USD

2. **حركة العمولة المنفصلة (تُعرض للأرباح والخسائر فقط):**
   - `customer_id`: الأرباح والخسائر
   - `amount`: 120
   - `commission`: 0
   - `is_commission_movement`: true
   - `related_commission_movement_id`: معرف الحركة الأساسية
   - `notes`: "عمولة من حركة رقم MV-XXXXX"

### لماذا هذا النظام؟

**الفوائد:**
- ✅ **لا تكرار في الحسابات:** كل عمولة تُحسب مرة واحدة فقط
- ✅ **أرصدة متطابقة:** جميع الصفحات تعرض نفس الرصيد
- ✅ **العمولة ظاهرة:** العمولة مرئية في الحركة الأساسية وفي حساب الأرباح والخسائر
- ✅ **تتبع دقيق:** يمكن معرفة مصدر كل عمولة عبر `related_commission_movement_id`

**قبل الإصلاح:**
- جلال يظهر برصيد -5120$ في صفحة العملاء
- جلال يظهر برصيد -5000$ في صفحة التفاصيل
- الفرق = 120$ (عمولة محسوبة مرتين)

**بعد الإصلاح:**
- جلال يظهر برصيد -5000$ في جميع الصفحات
- العمولة 120$ تظهر فقط في حساب الأرباح والخسائر
- لا تكرار في الحسابات

---

## الخلاصة

نظام العمولة التلقائي يوفر:
1. ✅ **دقة:** تسجيل تلقائي لجميع العمولات
2. ✅ **شفافية:** عرض واضح للمبالغ في السندات
3. ✅ **حماية:** منع حذف حساب النظام
4. ✅ **سهولة:** لا حاجة لتدخل يدوي
5. ✅ **توافق:** أرصدة متطابقة في جميع الصفحات
6. ✅ **لا تكرار:** حركات العمولة المنفصلة مستبعدة من الحسابات

**النظام جاهز للاستخدام ومُختبر بالكامل!** ✨

---

---

## تحديث 8 يناير 2026: خصم العمولة من رصيد العملاء

### المشكلة التي تم حلها

قبل هذا التحديث، كان رصيد العميل يُحسب بالمبلغ الكامل (amount) بدون خصم العمولة، مما يؤدي إلى:
- عدم دقة في الأرصدة المعروضة
- الرصيد لا يعكس المبلغ الصافي الحقيقي

### الحل المُطبق

تم تعديل VIEWs `customer_balances_by_currency` و `customer_balances` لخصم العمولة تلقائياً:

**المعادلة الجديدة:**
```
رصيد العميل = مجموع (المبلغ - العمولة) لكل حركة
```

### أمثلة على التحسينات

#### قبل التحديث:
- استلام 3000$ من أحمد بعمولة 60$
- رصيد أحمد: -3000$ ❌ (خطأ)

#### بعد التحديث:
- استلام 3000$ من أحمد بعمولة 60$
- رصيد أحمد: -2940$ ✅ (صحيح)
- العمولة 60$ في حساب الأرباح والخسائر ✅

### الفوائد

1. **دقة الأرصدة:** الرصيد المعروض = المبلغ الصافي الحقيقي
2. **وضوح العمولات:** العمولة مفصولة ومسجلة في حساب الأرباح والخسائر
3. **عدم الحاجة لتعديل البيانات:** التغيير على مستوى VIEWs فقط
4. **توافق كامل:** يعمل مع جميع أنواع الحركات (incoming, outgoing, internal transfers)
5. **سهولة التقارير:** التقارير المالية أصبحت أدق وأوضح

### الملفات المعنية

**قاعدة البيانات:**
- `supabase/migrations/20260108000000_fix_commission_deduction_in_customer_balances.sql`
- `supabase/migrations/20260108000001_fix_profit_loss_account_display_in_balances.sql`

**التوثيق:**
- `COMMISSION_SYSTEM_GUIDE.md` (هذا الملف)

---

**آخر تحديث:** 8 يناير 2026
